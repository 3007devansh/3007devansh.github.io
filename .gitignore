# ── Astro / Node (build artefacts & deps) ──────────────────────
node_modules/
dist/
.astro/
.DS_Store
Thumbs.db
*.log
.env
.env.*

# Stray empty dir left behind by a locked preview process (restructure)
/website/

# ── Large source assets kept locally / on Google Drive ─────────
# The web-optimised images the site actually uses ARE committed, under
# public/images/. These folders hold the heavy originals and are archival only.
/other-information/original-photos/
/other-information/enhanced-photos/
/other-information/college-shared/
.indexnow-key
